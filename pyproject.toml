[project]
name = "crew_ai_flow_trial"
version = "0.1.0"
description = "crew_ai_flow_trial using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "celery>=5.6.3",
    "crewai[tools]==1.14.3",
    "fastapi>=0.136.1",
    "redis>=7.4.0",
    "uvicorn>=0.46.0",
]

[project.scripts]
kickoff = "crew_ai_flow_trial.main:kickoff"
run_crew = "crew_ai_flow_trial.main:kickoff"
plot = "crew_ai_flow_trial.main:plot"
run_with_trigger = "crew_ai_flow_trial.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
